---
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  noindex?: boolean;
}

const {
  title,
  description = "Jyväskylä Cricket Club - A Finnish sports association founded in 2009, promoting cricket in Central Finland.",
  ogImage = "/images/jcc_og_image.jpg",
  noindex = false
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}

    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, Astro.site)} />
    <meta property="og:site_name" content="Jyväskylä Cricket Club" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={new URL(ogImage, Astro.site)} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />

    <!-- Fonts - System fonts for performance -->
    <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin />

    <!-- Structured Data -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "SportsOrganization",
      "name": "Jyväskylä Cricket Club",
      "alternateName": "JCC",
      "url": Astro.site,
      "logo": new URL("/images/jcc_logo.jpg", Astro.site),
      "foundingDate": "2009-06-03",
      "sport": "Cricket",
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Jyväskylä",
        "addressCountry": "FI"
      },
      "sameAs": [
        // Add social media links when available
      ]
    })} />

    <!-- Global Styles -->
    <style is:global>
      @import "../styles/global.css";
    </style>
  </head>

  <body>
    <!-- Skip to main content link for accessibility -->
    <a href="#main-content" class="skip-link sr-only-focusable">
      Skip to main content
    </a>

    <!-- Header Component -->
    <slot name="header" />

    <!-- Main Content -->
    <main id="main-content">
      <slot />
    </main>

    <!-- Footer Component -->
    <slot name="footer" />

    <style>
      /* Skip link styles */
      .skip-link {
        position: fixed;
        top: -100px;
        left: 50%;
        transform: translateX(-50%);
        background: var(--color-primary);
        color: white;
        padding: 1rem 2rem;
        text-decoration: none;
        z-index: var(--z-tooltip);
        border-radius: 0 0 var(--radius-md) var(--radius-md);
        font-weight: 600;
        box-shadow: var(--shadow-lg);
        transition: top var(--transition-base);
      }

      .skip-link:focus {
        top: 0;
      }

      /* Ensure main has proper spacing */
      main {
        min-height: 60vh;
      }

      /* Focus within improvements */
      main:focus-within {
        scroll-margin-top: 2rem;
      }
    </style>
  </body>
</html>
